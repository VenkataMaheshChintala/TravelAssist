# Spring Boot application configuration
spring.application.name=TGSRTC

# Server port
server.port=8080

# PostgreSQL datasource
spring.datasource.url=jdbc:postgresql://localhost:5432/hyderabad_transit
spring.datasource.username=maheshchintala
spring.datasource.password=
spring.datasource.driver-class-name=org.postgresql.Driver

# Disable JPA/Hibernate (we use plain JdbcTemplate)
spring.autoconfigure.exclude=org.springframework.boot.autoconfigure.orm.jpa.HibernateJpaAutoConfiguration

# Do not run any SQL init scripts
spring.sql.init.mode=never
